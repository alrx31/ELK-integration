# Пример файла с переменными окружения для ELK стека
# Скопируйте этот файл в .env и измените значения

# Пароль для пользователя elastic в Elasticsearch
# ВАЖНО: Измените этот пароль для production!
ELASTIC_PASSWORD=test_password_123

# Имя пользователя Elasticsearch (по умолчанию elastic)
ELASTICSEARCH_USER=elastic

# Пароль для пользователя kibana_system (для Kibana)
# ВАЖНО: Установите пароль после первого запуска Elasticsearch:
# docker exec elasticsearch bin/elasticsearch-reset-password -u kibana_system --batch
KIBANA_SYSTEM_PASSWORD=changeme

# Настройки памяти для Elasticsearch (опционально)
# ES_JAVA_OPTS=-Xms1g -Xmx1g

# Настройки памяти для Logstash (опционально)
# LS_JAVA_OPTS=-Xmx512m -Xms512m
